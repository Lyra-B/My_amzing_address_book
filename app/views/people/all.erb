<html>
<head>
	<title></title>
</head>
<body>
	<table>
	<%=
	@people.each do |person|
		person.attributes.map{|key,value|
		"<th>#{key}</th>}"}.join("")
		person.attributes.map{|key,value|
		"<td>#{value}</td>}"}.join("")
  end
	%>
	</table>
</body>
</html>