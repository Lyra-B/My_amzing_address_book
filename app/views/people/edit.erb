<% form_for @person, url_for(:person, :update, :id => @person.id), :method => "put" do |form| %>

	<%= partial 'people/form', :locals => {:form => form} %>
	<%= partial 'people/type_specification', :locals => {:form => form,  :person => @person} %>

	<%= form.submit "Update" %>

<% end %>

